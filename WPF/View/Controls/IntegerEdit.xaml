<UserControl x:Class="SmartPert.View.Controls.IntegerEdit"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SmartPert.View.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="100">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBox x:Name="MyEdit" Grid.RowSpan="2" LostFocus="MyEdit_LostFocus" Text="1" TextWrapping="NoWrap" FontSize="20" TextAlignment="Right"/>
        <Button Content="▲" Grid.Column="2" Click="Up_Click"/>
        <Button Content="▼" Grid.Column="2" Grid.Row="2" Click="Down_Click"/>
    </Grid>
</UserControl>
