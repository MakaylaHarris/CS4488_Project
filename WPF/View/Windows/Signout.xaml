<Window x:Class="SmartPert.View.Windows.Signout"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SmartPert.View.Windows"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal" 
        TextOptions.TextRenderingMode="Auto"        
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        mc:Ignorable="d"
        Title="Signout" Height="150" Width="450">
    <Grid>
        <StackPanel><Label HorizontalAlignment="Center" Margin="0,20,0,0">Are you sure you want to sign out?</Label>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Margin="5,5,5,5" Style="{StaticResource MaterialDesignRaisedLightButton}" Click="Yes_Click">YES</Button>
            <Button Margin="5,5,5,5" Style="{StaticResource MaterialDesignRaisedLightButton}" Click="No_Click">NO</Button>
        </StackPanel>
        </StackPanel>
    </Grid>
</Window>
